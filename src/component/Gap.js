import {StyleSheet, View} from 'react-native';
import React from 'react';

export default function Gap({width, height}) {
  return <View width={width} height={height}></View>;
}

const styles = StyleSheet.create({});
